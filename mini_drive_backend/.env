# Local Development Environment Variables
# This file is used for local development only
# Production environment variables are set directly in Render.com dashboard

# Django Core Settings
DEBUG=True
SECRET_KEY=django-insecure-eg4_v90&e(k6++8%tt0f%10d^!!*_8f3(en^hk329d0hr-2sqm

# Allowed Hosts (for local development)
ALLOWED_HOSTS=localhost,127.0.0.1,10.0.2.2,*

# CORS Settings (for React Native development)
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://127.0.0.1:8081,http://10.0.2.2:8081
CORS_ALLOW_ALL_ORIGINS=True

# Database (uses SQLite for local development)
# DATABASE_URL is not set, so Django will use SQLite by default